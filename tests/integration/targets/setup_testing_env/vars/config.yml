---
dns_servers_to_create:
  - name: "technitium01{{ lookup('env', 'CONTAINER_SUFFIX') | default('') }}"
    http_port: "{{ lookup('env', 'HTTP_PORT_1') | default('5381') }}"
    https_port: "{{ lookup('env', 'HTTPS_PORT_1') | default('53441') }}"
    dns_server_domain: "{{ lookup('env', 'DNS_SERVER_DOMAIN_1') | default('testing01.local') }}"
    image_tag: "latest"
  - name: "technitium02{{ lookup('env', 'CONTAINER_SUFFIX') | default('') }}"
    http_port: "{{ lookup('env', 'HTTP_PORT_2') | default('5382') }}"
    https_port: "{{ lookup('env', 'HTTPS_PORT_2') | default('53442') }}"
    dns_server_domain: "{{ lookup('env', 'DNS_SERVER_DOMAIN_2') | default('testing02.local') }}"
    image_tag: "latest"
  - name: "technitium03{{ lookup('env', 'CONTAINER_SUFFIX') | default('') }}"
    http_port: "{{ lookup('env', 'HTTP_PORT_3') | default('5383') }}"
    https_port: "{{ lookup('env', 'HTTPS_PORT_3') | default('53443') }}"
    dns_server_domain: "{{ lookup('env', 'DNS_SERVER_DOMAIN_3') | default('testing03.local') }}"
    image_tag: "13.4.0"
  - name: "technitium_cluster_primary{{ lookup('env', 'CONTAINER_SUFFIX') | default('') }}"
    http_port: "{{ lookup('env', 'HTTP_PORT_4') | default('5384') }}"
    https_port: "{{ lookup('env', 'HTTPS_PORT_4') | default('53444') }}"
    dns_server_domain: "{{ lookup('env', 'DNS_SERVER_DOMAIN_4') | default('testing04.local') }}"
    image_tag: "latest"
  - name: "technitium_cluster_secondary01{{ lookup('env', 'CONTAINER_SUFFIX') | default('') }}"
    http_port: "{{ lookup('env', 'HTTP_PORT_5') | default('5385') }}"
    https_port: "{{ lookup('env', 'HTTPS_PORT_5') | default('53445') }}"
    dns_server_domain: "{{ lookup('env', 'DNS_SERVER_DOMAIN_5') | default('testing05.local') }}"
    image_tag: "latest"
  - name: "technitium_cluster_secondary02{{ lookup('env', 'CONTAINER_SUFFIX') | default('') }}"
    http_port: "{{ lookup('env', 'HTTP_PORT_6') | default('5386') }}"
    https_port: "{{ lookup('env', 'HTTPS_PORT_6') | default('53446') }}"
    dns_server_domain: "{{ lookup('env', 'DNS_SERVER_DOMAIN_6') | default('testing06.local') }}"
    image_tag: "latest"

dns_server_admin_password: "topsecret"
dns_server_domain: "testing.local"

t1_zones_to_create:
  - name: secondary.{{ lookup('env', 'TESTING_SUFFIX') | default('example.com') }}
    type: Primary
  - name: stub.{{ lookup('env', 'TESTING_SUFFIX') | default('example.com') }}
    type: Primary

t2_zones_to_create:
  - name: update-dnskey-ttl-test1.{{ lookup('env', 'TESTING_SUFFIX') | default('example.com') }}
    type: Primary
